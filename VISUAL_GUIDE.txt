# 🎨 ВИЗУАЛЬНАЯ СХЕМА DOCKER ДЕПЛОЯ

```
╔════════════════════════════════════════════════════════════════════════════╗
║                     АРХИТЕКТУРА DOCKER ДЕПЛОЯ БОТА                        ║
╚════════════════════════════════════════════════════════════════════════════╝


                            ┌─────────────────────┐
                            │   Telegram Server   │
                            │ (api.telegram.org)  │
                            └────────────┬────────┘
                                         │ polling updates
                                         │
                    ╔════════════════════╩════════════════════╗
                    │                                         │
                    ▼                                         ▼
        ┌─────────────────────┐                  ┌─────────────────────┐
        │  Google Sheets API  │                  │  Remote Server      │
        │  (monitoring bars)  │◄─────────────────│  (Linux Ubuntu)     │
        └─────────────────────┘                  │  IP: 192.168.1.100  │
                                                 └─────────────┬───────┘
                                                               │
                                                               │
                    ╔══════════════════════════════════════════╩════════╗
                    │                                                  │
                    │        /opt/antibars-bot/                        │
                    │   ┌────────────────────────────┐               │
                    │   │    Docker Compose Setup    │               │
                    │   └────────────────────────────┘               │
                    │                                                │
    ┌───────────────┴───────────────┐                               │
    │                               │                               │
    ▼                               ▼                               │
┌─────────────┐            ┌──────────────────────────────────────┐│
│  docker-    │  manages   │  Container: antibars-bot            ││
│ compose.yml │──────────▶ │  ┌──────────────────────────────┐   ││
└─────────────┘            │  │  Python 3.11 Environment     │   ││
                           │  ├──────────────────────────────┤   ││
                           │  │  ✓ aiohttp                   │   ││
                           │  │  ✓ gspread (Google Sheets)   │   ││
                           │  │  ✓ beautifulsoup4            │   ││
                           │  │  ✓ requests                  │   ││
                           │  └──────────────────────────────┘   ││
                           │  ┌──────────────────────────────┐   ││
                           │  │  Main Process:               │   ││
                           │  │  python -m lab4.async_bot    │   ││
                           │  │  ├─ async polling            │   ││
                           │  │  ├─ bars watcher (БД)        │   ││
                           │  │  └─ message handlers         │   ││
                           │  └──────────────────────────────┘   ││
                           │  ┌──────────────────────────────┐   ││
                           │  │  Environment Variables:      │   ││
                           │  │  - BOT_TOKEN (from .env)     │   ││
                           │  │  - PYTHONUNBUFFERED=1        │   ││
                           │  └──────────────────────────────┘   ││
                           └──────────────────────────────────────┘│
                    │                                                │
                    └──────────────────────────────────────────────┘
                                     │
        ╔════════════════════════════╩════════════════════════════╗
        │              VOLUMES (Persistent Storage)              │
        ╠════════════════════════════════════════════════════════╣
        │                                                         │
        │  ./data/                                                │
        │  ├── antibars-credentials.json  (read-only)            │
        │  └── bars_db.sqlite              (database)            │
        │                                                         │
        │  ./logs/                                                │
        │  └── bot.log                    (application logs)     │
        │                                                         │
        └─────────────────────────────────────────────────────────┘


╔════════════════════════════════════════════════════════════════════════════╗
║                        LIFECYCLE (Жизненный цикл)                         ║
╚════════════════════════════════════════════════════════════════════════════╝

1. LOCAL MACHINE (Локальная машина):
   ┌─────────────────────────────────────────┐
   │ git push → GitHub                       │
   │ (код + Docker конфиги)                  │
   └──────────────────┬──────────────────────┘
                      │
2. REMOTE SERVER (Удалённый сервер):
   ┌──────────────────▼──────────────────┐
   │ git clone (скачиваем код)           │
   └──────────────────┬──────────────────┘
                      │
   ┌──────────────────▼──────────────────┐
   │ docker-compose build                │
   │ (собираем образ из Dockerfile)      │
   └──────────────────┬──────────────────┘
                      │
   ┌──────────────────▼──────────────────┐
   │ docker-compose up -d                │
   │ (запускаем контейнер)               │
   └──────────────────┬──────────────────┘
                      │
   ┌──────────────────▼──────────────────┐
   │ Контейнер запущен                   │
   │ restart: unless-stopped             │
   │ (автоперезапуск при сбое)           │
   └──────────────────┬──────────────────┘
                      │
   ┌──────────────────▼──────────────────┐
   │ Telegram polling активен            │
   │ Google Sheets monitoring активен    │
   │ БД инициализирована                 │
   └─────────────────────────────────────┘


╔════════════════════════════════════════════════════════════════════════════╗
║                    FILE STRUCTURE (Структура файлов)                       ║
╚════════════════════════════════════════════════════════════════════════════╝

/opt/antibars-bot/
├── Dockerfile                   ← Как собрать образ
├── docker-compose.yml           ← Как запустить контейнер
├── .dockerignore                ← Исключаемые файлы
├── requirements.txt             ← Python зависимости
├── .env                         ← Переменные окружения (на сервере)
├── .env.example                 ← Шаблон .env
│
├── lab4/
│   ├── async_bot.py            ← Главный файл бота
│   ├── constants.py            ← Конфиги и параметры
│   ├── bars_db.py              ← SQLite БД операции
│   ├── bars_watcher.py         ← Мониторинг Google Sheets
│   ├── google_sheets_client.py ← Интеграция с Google API
│   └── sync_bot.py             ← Синхронные функции
│
├── data/                        ← VOLUME: Персистентные данные
│   ├── antibars-credentials.json    (Google API ключ)
│   └── bars_db.sqlite              (SQLite база)
│
└── logs/                        ← VOLUME: Логи приложения
    └── bot.log


╔════════════════════════════════════════════════════════════════════════════╗
║                  COMMUNICATION FLOW (Поток коммуникации)                    ║
╚════════════════════════════════════════════════════════════════════════════╝

User in Telegram          Docker Container              APIs
     │                            │                      │
     │  /weather Москва           │                      │
     ├──────────────────────────▶ │                      │
     │                    [polling]│                      │
     │                            ├─ HTTP request ───────▶ Telegram API
     │                            │ getUpdates()          │
     │                            │                      │
     │ OpenWeather Data ◀─────────├──────────────────────┤
     │ {"temp": 5°C}              │ weather request       │
     │                            │                      │
     │ 🌡️ Москва: 5°C ◀──────────┤                      │
     │                            │ sendMessage()        │
     │                            │                      │
     │                            ▼ Google Sheets updates
     │                            (background task)
     │
     │  /set_isu 123456           │
     ├──────────────────────────▶ │
     │                            ├─ store in SQLite
     │                            │  (data/bars_db.sqlite)
     │ ✅ ИСУ сохранён ◀──────────┤
     │                            │


╔════════════════════════════════════════════════════════════════════════════╗
║                    RESOURCE LIMITS (Ограничения ресурсов)                  ║
╚════════════════════════════════════════════════════════════════════════════╝

Container: antibars-bot
├── Memory Limit:     512MB   (максимум памяти)
├── Memory Reserve:   256MB   (зарезервированно)
├── CPU Limit:        1 core  (максимум CPU)
└── CPU Reserve:      0.5 core(зарезервировано)

Logging:
├── Driver:           json-file
├── Max Size:         10MB (максимум одного файла)
└── Max Files:        3    (максимум файлов)

Restart Policy:
└── unless-stopped    (перезапускается при сбое/перезагрузке)


╔════════════════════════════════════════════════════════════════════════════╗
║                   MONITORING & DEBUGGING (Мониторинг)                      ║
╚════════════════════════════════════════════════════════════════════════════╝

Commands for monitoring:

docker-compose ps
  └─ Показывает статус контейнера

docker-compose logs -f
  └─ Логи в реальном времени

docker stats antibars-bot
  └─ Использование CPU, памяти, сети

docker-compose exec antibars-bot bash
  └─ Вход в контейнер для отладки

docker-compose exec antibars-bot sqlite3 data/bars_db.sqlite ".schema"
  └─ Просмотр структуры БД


╔════════════════════════════════════════════════════════════════════════════╗
║              DEPLOYMENT WORKFLOW (Процесс развёртывания)                   ║
╚════════════════════════════════════════════════════════════════════════════╝

Day 1 - First Deployment:
  git push
    ↓
  ssh to server
    ↓
  git clone
    ↓
  docker-compose build
    ↓
  mkdir data logs
    ↓
  scp credentials
    ↓
  cp .env.example .env
    ↓
  nano .env (вставляем BOT_TOKEN)
    ↓
  docker-compose up -d
    ↓
  docker-compose logs -f (проверяем логи)
    ↓
  ✅ BOT READY!


Day 2+ - Maintenance:
  docker-compose logs -f         (проверяем логи)
    ↓
  docker stats                   (смотрим ресурсы)
    ↓
  [Code update?]
    git pull
    docker-compose build
    docker-compose restart


Incident - Bot Down:
  docker-compose logs            (смотрим, почему упал)
    ↓
  docker-compose exec ... bash   (отлаживаем)
    ↓
  Fix issue
    ↓
  docker-compose restart         (перезапускаем)
    ↓
  ✅ BACK ONLINE!


╔════════════════════════════════════════════════════════════════════════════╗
║                    SECURITY (Безопасность)                                 ║
╚════════════════════════════════════════════════════════════════════════════╝

✅ Credentials Protection:
   - antibars-credentials.json в docker volume (не в image!)
   - Read-only mount (только чтение)
   - chmod 600 (только владелец)
   - Не коммитится в git

✅ Environment Variables:
   - BOT_TOKEN в .env (не в коде!)
   - .env не коммитится в git
   - Берётся из docker-compose.yml

✅ Container Isolation:
   - Работает в отдельном namespace
   - Ограничены ресурсы (512MB памяти)
   - Нет прямого доступа к хост-системе
   - read-only где возможно

✅ Network:
   - Отдельная docker-сеть
   - Изолирован от других контейнеров


╔════════════════════════════════════════════════════════════════════════════╗
║                    SUCCESS INDICATORS (Признаки успеха)                    ║
╚════════════════════════════════════════════════════════════════════════════╝

✅ Все признаки того, что всё работает:

docker-compose ps:
  STATUS: Up 2 minutes (контейнер запущен)

docker-compose logs:
  Async echo bot started
  БД инициализирована
  BARS watcher запущен (интервал: 30s)

Telegram:
  /weather Москва         → ✅ Отвечает с температурой
  /quote                  → ✅ Отправляет случайную цитату
  /headlines              → ✅ Показывает новости
  /set_isu 123456         → ✅ Сохраняет в БД

Database:
  ls -la data/bars_db.sqlite → ✅ Файл существует
  sqlite3 ... SELECT * FROM subscriptions → ✅ Есть подписчики


🎉 ПОЗДРАВЛЯЕМ! БОТ ПОЛНОСТЬЮ ГОТОВ К РАБОТЕ!
