# 🎓 ПРИМЕР РЕАЛЬНОГО ДЕПЛОЯ НА УДАЛЁННЫЙ СЕРВЕР

Это пошаговый пример, как развернуть бота на удалённом Linux сервере. Используются реальные команды, которые можешь копировать-пастить.

---

## 📌 Сценарий

- **Локальная машина:** Windows/Mac с WSL или Linux, где хранится код
- **Удалённый сервер:** Linux (Ubuntu 22.04) с IP `192.168.1.100`
- **SSH пользователь:** `deploy`
- **Директория на сервере:** `/opt/antibars-bot`
- **BOT_TOKEN:** `8530256771:AAFgOsnFR3sAq-SUQ1vVcbnf2zt0kLg_Cf4`

---

## 🔧 ШАГИ НА ЛОКАЛЬНОЙ МАШИНЕ

### Шаг 1: Подготавливаем проект

```bash
# Переходим в директорию проекта
cd ~/projects/HammonDDDDD

# Проверяем, что все файлы Docker есть
ls -la Dockerfile docker-compose.yml requirements.txt .dockerignore .env.example

# Проверяем наличие credentials
ls -la lab4/antibars-credentials.json

# Проверяем наличие кода
ls -la lab4/*.py
```

### Шаг 2: Коммитим изменения в git

```bash
# Убедиться, что .gitignore содержит:
cat .gitignore
# Должны быть:
# .env
# data/
# logs/

# Если чего-то нет, добавляем
echo ".env" >> .gitignore
echo "data/" >> .gitignore
echo "logs/" >> .gitignore

# Добавляем все файлы (кроме игнорируемых)
git add .

# Коммитим
git commit -m "Add Docker deployment configuration"

# Пушим на GitHub
git push origin main
```

### Шаг 3: Готовим credentials (если еще не на сервере)

```bash
# На локальной машине копируем credentials
# Найди файл lab4/antibars-credentials.json и приготовь его

# Позже отправим через SCP (более безопасно, чем коммитить в git!)
```

---

## 🖥️ ШАГИ НА УДАЛЁННОМ СЕРВЕРЕ

### Шаг 4: Подключаемся к серверу

```bash
# На локальной машине подключаемся по SSH
ssh deploy@192.168.1.100

# Если это первое подключение, введи пароль
# После подключения ты в shell сервера
```

### Шаг 5: Проверяем, что Docker установлен

```bash
# На сервере проверяем Docker
docker --version
# Должно вывести: Docker version 20.10.x

docker-compose --version
# Должно вывести: docker-compose version 2.x.x

# Если не установлены, выполни:
# curl -fsSL https://get.docker.com | sudo sh
# sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
# sudo chmod +x /usr/local/bin/docker-compose
```

### Шаг 6: Готовим директорию проекта

```bash
# На сервере переходим в /opt
cd /opt

# Создаём директорию проекта
sudo mkdir -p antibars-bot
sudo chown deploy:deploy antibars-bot  # Даём права текущему пользователю

# Переходим в неё
cd antibars-bot
```

### Шаг 7: Клонируем репозиторий

```bash
# На сервере клонируем репо (если ты загрузил через git)
git clone https://github.com/твой-юзер/HammonDDDDD.git .

# Если получить ошибку "fatal: destination path '.' already exists"
# Это нормально, продолжаем

# Переходим в lab4
cd lab4

# Проверяем, что файлы скопировались
ls -la Dockerfile docker-compose.yml requirements.txt
```

### Шаг 8: Отправляем credentials на сервер

```bash
# ВАЖНО! Это делаем с локальной машины (из новой сессии SSH)
# (Не закрывай текущее соединение с сервером, открой новый терминал)

# На локальной машине отправляем credentials через SCP
scp ~/projects/HammonDDDDD/lab4/antibars-credentials.json deploy@192.168.1.100:/opt/antibars-bot/data/

# Если директория data/ еще не создана, создаём на сервере
# (В текущей сессии на сервере)
mkdir -p data

# После отправки проверяем на сервере
ls -la data/antibars-credentials.json
# Должен быть файл

# Устанавливаем правильные права (только владелец может читать)
chmod 600 data/antibars-credentials.json
```

### Шаг 9: Создаём файл переменных окружения

```bash
# На сервере в директории /opt/antibars-bot/

# Копируем шаблон
cp .env.example .env

# Редактируем .env
nano .env

# Должно быть:
# BOT_TOKEN=8530256771:AAFgOsnFR3sAq-SUQ1vVcbnf2zt0kLg_Cf4
# PYTHONUNBUFFERED=1

# Сохраняем: Ctrl+O, Enter, Ctrl+X
```

### Шаг 10: Создаём необходимые директории

```bash
# На сервере в /opt/antibars-bot/

# Создаём директории для БД и логов
mkdir -p data logs

# Проверяем структуру
ls -la
# Должны быть: Dockerfile, docker-compose.yml, requirements.txt, data/, logs/, .env

# Проверяем, что credentials на месте
ls -la data/antibars-credentials.json
```

---

## 🐳 ЗАПУСК DOCKER

### Шаг 11: Собираем Docker образ

```bash
# На сервере в /opt/antibars-bot/

# Собираем образ (может занять 2-5 минут)
docker-compose build

# Вывод должен показать:
# Successfully built antibars-bot:latest
# Successfully tagged antibars-bot:latest
```

### Шаг 12: Запускаем контейнер

```bash
# На сервере в /opt/antibars-bot/

# Запускаем в фоновом режиме (-d = detach)
docker-compose up -d

# Проверяем статус
docker-compose ps

# Вывод должен показать контейнер в статусе "Up"
# CONTAINER ID   IMAGE                COMMAND                  STATUS
# abc123def456   antibars-bot:latest  "python -m lab4.asy…"    Up 2 seconds
```

### Шаг 13: Проверяем логи

```bash
# На сервере в /opt/antibars-bot/

# Смотрим логи (последние 50 строк)
docker-compose logs --tail=50 antibars-bot

# Смотрим логи в реальном времени (live tail)
docker-compose logs -f antibars-bot

# Ищи такие строки:
# Async echo bot started
# БД инициализирована
# BARS watcher запущен

# Если видишь эти строки - ВСЁ РАБОТАЕТ! 🎉

# Выход из live logs: Ctrl+C
```

---

## ✅ ПРОВЕРКА РАБОТОСПОСОБНОСТИ

### Шаг 14: Тестируем бота в Telegram

Открой Telegram и найди своего бота (например, @antibars_bot), отправь команды:

```
/weather Москва
/quote
/headlines
/set_isu 123456
```

Если бот отвечает на все команды - **ДЕПЛОЙ УСПЕШЕН!** 🚀

---

## 📊 МОНИТОРИНГ И УПРАВЛЕНИЕ

### После успешного запуска

```bash
# На сервере проверяем статус контейнера
docker-compose ps

# Смотрим использование памяти/CPU
docker stats antibars-bot

# Просмотр последних 100 строк логов
docker-compose logs --tail=100 antibars-bot

# Перезапуск контейнера (если нужно)
docker-compose restart

# Остановка контейнера (если нужно)
docker-compose down

# Запуск снова
docker-compose up -d
```

---

## 📝 ОБНОВЛЕНИЕ КОДА

Если обновил код на GitHub:

```bash
# На сервере в /opt/antibars-bot/

# Обновляем из git
git pull

# Пересобираем образ (может занять 2-5 минут)
docker-compose build --no-cache

# Перезапускаем контейнер
docker-compose restart

# Проверяем новые логи
docker-compose logs -f antibars-bot
```

---

## 💾 РЕЗЕРВНОЕ КОПИРОВАНИЕ БД

```bash
# На сервере в /opt/antibars-bot/

# Создаём backup (внутри контейнера копируем файл)
docker-compose exec antibars-bot cp data/bars_db.sqlite data/backup_$(date +%Y%m%d_%H%M%S).sqlite

# Копируем на локальную машину (из новой сессии SSH на локальной машине)
scp deploy@192.168.1.100:/opt/antibars-bot/data/backup_*.sqlite ~/backups/

# Проверяем на локальной машине
ls -la ~/backups/backup_*.sqlite
```

---

## 🔄 АВТОЗАПУСК ПРИ ПЕРЕЗАГРУЗКЕ

Docker Compose уже имеет `restart: unless-stopped`, поэтому контейнер автоматически перезапустится при перезагрузке сервера!

Проверяем:

```bash
# На сервере рестартуем контейнер
docker-compose restart

# Проверяем статус
docker-compose ps

# После перезагрузки сервера проверяем снова
# ssh deploy@192.168.1.100
# cd /opt/antibars-bot
# docker-compose ps  # Должен быть Up!
```

---

## 🎯 ЧАСТО ИСПОЛЬЗУЕМЫЕ КОМАНДЫ

```bash
# Быстрые команды для ежедневного использования

# Запустить контейнер
docker-compose up -d

# Остановить контейнер
docker-compose down

# Перезагрузить бота
docker-compose restart

# Смотреть логи (следить за ошибками)
docker-compose logs -f antibars-bot

# Обновиться и перезагрузить
git pull && docker-compose build && docker-compose restart

# Проверить статус
docker-compose ps

# Войти в контейнер для отладки
docker-compose exec antibars-bot bash
```

---

## 🚨 ЕСЛИ ЧТО-ТО СЛОМАЛОСЬ

```bash
# 1. Проверяем логи (первое, что нужно сделать)
docker-compose logs antibars-bot | tail -100

# 2. Проверяем, что credentials на месте
ls -la data/antibars-credentials.json

# 3. Проверяем .env файл
cat .env

# 4. Проверяем интернет внутри контейнера
docker-compose exec antibars-bot ping 8.8.8.8

# 5. Входим в контейнер и пытаемся запустить вручную
docker-compose exec antibars-bot bash
python3 -m lab4.async_bot

# 6. Ядерный вариант (полная очистка и переезапуск)
docker-compose down -v
rm -rf data/bars_db.sqlite
docker-compose build --no-cache
docker-compose up -d
```

---

## 📚 ПОЛЕЗНЫЕ ССЫЛКИ

- Смотри **DEPLOYMENT_GUIDE.md** для подробного разбора
- Смотри **FAQ_DOCKER.md** для решения проблем
- Смотри **docker_commands_reference.sh** для справки по командам

---

## 🎉 ПОЗДРАВЛЯЕМ!

Если дошёл до сюда и всё работает - **МОЛОДЕЦ!** 🚀

Твой Telegram бот теперь работает 24/7 на удалённом сервере!

```
┌─────────────────────────────┐
│  BOT DEPLOYED SUCCESSFULLY  │
│  Status: ✅ RUNNING         │
│  Uptime: 24/7               │
│  Location: production       │
└─────────────────────────────┘
```

Пиши команды боту и наслаждайся результатом! 🤖
